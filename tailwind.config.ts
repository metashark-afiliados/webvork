// tailwind.config.ts

/** @type {import('tailwindcss').Config} */
module.exports = {
  // Ya no se necesitan las claves `darkMode`, `content`, `theme` y `plugins`.
  // Tailwind v4 las inferirá o las leerá desde `globals.css`.
  // Dejamos el archivo por si en el futuro se necesitan configuraciones
  // que aún requieran JS, aunque es poco probable.
};
